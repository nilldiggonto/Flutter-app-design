class NoteForListing {
  String? noteId;
  String? noteTitle;
  DateTime? createDateTime;
  DateTime? lastEditTime;

  NoteForListing(
      {this.noteId, this.noteTitle, this.createDateTime, this.lastEditTime});
}
